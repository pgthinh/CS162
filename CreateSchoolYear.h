#ifndef CreateSchoolYear_h
#define CreateSchoolYear_h
#include "supportfunction.h"
#include "Console.h"

bool readDate(Time& date);
bool readInt(int& number);
void createSchoolYear();
void createSemester();

#endif
